# DECISIONS — MD-Viewer-ALL

> G2 인터뷰 산출물. 재논의 방지용. 뒤집으려면 이 파일을 근거로 명시적으로 재결정한다.

## 2026-07-24 — 초기 P0 결정 (G2 인터뷰)

| # | 항목 | 결정 | 근거 / 비고 |
|---|------|------|-------------|
| D1 | 플랫폼 범위 | **macOS + Windows** | MacBook + 연구소 PC 양쪽 사용. Linux 제외. → **2026-07-29 D13에서 개정** |
| D2 | 앱 셸 스택 | **Electron** | 사용자 선택. OS 간 렌더링 완전 동일(번들 Chromium)이 "작성자 의도 재현" 목표와 정합. Tauri(경량) 추천안 기각 — 크로스 OS 렌더링 일관성과 생태계 검증을 우선. |
| D3 | 마크다운 범위 | **GFM + KaTeX 수식 + Mermaid + 코드 하이라이트 + Obsidian 확장(`[[위키링크]]`, 콜아웃)** | 연구 문서 + Obsidian 볼트 문서 열람 대응. |
| D4 | 핵심 UX | **폴더 트리(사이드바) + 탭 다중 열람** | 프로젝트 문서 뭉치 열람이 주 시나리오. |

## 2026-07-24 — G3 수렴 및 잔여 결정

| # | 항목 | 결정 | 근거 / 비고 |
|---|------|------|-------------|
| D5 | 시각 방향 | **C. IDE 다크** (Tokyo Night 계열) | G3 목업 4안 반응. 토큰은 [[DESIGN]]에 고정. 라이트 테마 전환 기본 탑재. |
| D6 | 파일 감시 | **포함** (chokidar) | 외부 편집기 수정 시 자동 재렌더링. 편집기+뷰어 병행 사용 패턴 대응. |
| D7 | Electron 재확정 | 단독 실행 프로그램 확인 후 유지 | 사용자 질의(웹 서버 방식 아닌지) → 셸+내장 웹뷰 구조 설명 후 "그대로 진행" 승인. |

## 2026-07-24 — v0.2 범위

| # | 항목 | 결정 | 근거 / 비고 |
|---|------|------|-------------|
| D8 | PDF 지원 | **양방향 모두** — MD→PDF 내보내기(`printToPDF` + 인쇄 CSS) + PDF 열람(Chromium 내장 뷰어 iframe) | 사용자 선택("둘 다"). 외부 의존성 추가 없음, 오프라인 동작 유지. |

## 2026-07-24 — v0.3.0 기능 3종 (자율 진행)

| # | 항목 | 결정 | 근거 / 비고 |
|---|------|------|-------------|
| D9 | 아웃라인 패널 | 사이드바 하단 섹션(접기 가능) | 별도 리사이저 없이 기존 사이드바 폭 재사용(VS Code 메타포). 헤딩 클릭 점프 + 스크롤 위치 추적. |
| D10 | 세션 복원 | localStorage에 폴더·탭·활성 저장, 시작 시 복원 | folder:openPath IPC로 대화상자 없이 스캔. 삭제된 파일은 건너뜀. |
| D11 | 전체 검색 | ⌘⇧F, main 프로세스 줄 단위 스캔, 상한 300건 | .md만 대상(PDF 제외). 결과 클릭 → 문서 열고 ⌘F로 하이라이트 재사용. |
| D12 | Finder .md 연결 | electron-builder fileAssociations + open-file(mac)/argv(win) + 단일 인스턴스 | 설치 빌드에서만 활성. dev 모드 검증 불가 → 설치 후 사용자 확인 필요. 폴더 미오픈 시 파일의 상위 폴더를 트리로 자동 오픈. |

## 2026-07-29 — 플랫폼 확대와 연결 그래프

| # | 항목 | 결정 | 근거 / 비고 |
|---|------|------|-------------|
| D13 | 플랫폼 범위 | **macOS + Windows + Linux** (D1 개정) | Linux 추가 비용이 워크플로 매트릭스 한 줄 + electron-builder 타깃 설정뿐. AppImage(배포판 무관) + deb 두 형식. 로컬에서 패키징 성공 검증. |
| D14 | 연결 그래프 | **캔버스 + 자체 구현 힘-지향 레이아웃**, 전체 화면 오버레이 | d3-force 등 외부 의존성 대신 자체 구현 — 오프라인·번들 크기 요건([[DESIGN#4. 금지 목록|금지 목록]])과 통제권 확보. 사이드바 축소판이 아니라 전체 화면으로 둔 이유는 라벨 가독성과 조작(드래그·줌) 공간. |
| D15 | 그래프 진입점 | 메뉴 + **사이드바 버튼** 병행 | 메뉴 전용은 발견성이 낮고 자동 검증도 불가(OS 단축키는 합성 이벤트로 발동하지 않음). |

## 2026-07-29 — 오프라인 제약의 적용 범위 정정

| # | 항목 | 결정 | 근거 / 비고 |
|---|------|------|-------------|
| D16 | 오프라인 제약 범위 | **렌더링 자산 번들에 한정.** 앱 전체의 "네트워크 요청 금지"는 철회 | 전역 지침 §5의 온프레미·망분리·GPU 전제는 **LLM·추론 시스템 대상**이며, 어디서나 돌아가는 범용 앱에는 적용하지 않는다(사용자 정정). 렌더링 자산 번들은 오프라인 교리가 아니라 **충실도·견고성** 근거로 유지. |
| D17 | 업데이트 | Windows·Linux(AppImage) **자동 설치**, macOS·deb는 **알림만** | macOS는 Apple 코드 서명이 없으면 Squirrel이 업데이트를 거부한다(하드 제약). deb는 패키지 관리자 영역. 자동 확인은 끌 수 있게 한다. |

## 2026-07-30 — 파일 단위 열기

| # | 항목 | 결정 | 근거 / 비고 |
|---|------|------|-------------|
| D18 | 파일 열기 추가 | **`⌘O` = 파일 열기, `⇧⌘O` = 폴더 열기**로 재배치 | 폴더 열기만 있어 문서 하나를 보려면 폴더를 열어야 했다(사용자 지적). 대부분의 앱에서 `⌘O`는 파일 열기이므로 관례를 따르고, 기존 단축키 변경을 문서에 명시. 다중 선택 지원. 폴더가 이미 열려 있으면 트리는 유지하고 탭만 추가한다. |
| D19 | 명령줄 인자 | 모든 OS에서 `앱 문서.md` 형태로 열기 지원 | 기존에는 Windows에서만 argv를 처리했다. CLI 실행 편의 + 파일→탭 경로의 자동 검증 수단이 된다. |

## 2026-08-01 — 편집 기능 도입 (G2 인터뷰)

"읽기 전용 뷰어"라는 [[DECISIONS|D6]]의 전제(외부 편집기 + 파일 감시 병행)를 부분적으로 뒤집는
결정이므로 인터뷰 10문항을 거쳤다. 논증은 [[RATIONALE#2026-08-01 — 편집: 상태 기계를 없애는 선택이 곧 안전 설계였다|RATIONALE]] 참조.

| # | 항목 | 결정 | 근거 / 비고 |
|---|------|------|-------------|
| D20 | 편집 범위 | **① 오타·짧은 수정 + ② 메모 덧붙이기까지.** 파일 생성·이름 변경·삭제, 위키링크 리네임 전파는 **범위 밖** | 사용자가 이 앱을 주 저작 도구로 쓸 의사가 없음을 확인. 편집기 복잡도의 절반 이상이 여기서 잘려 나간다. |
| D21 | 편집 형태 | **인라인 블록 편집 + 소스 모드 토글 병행** | 읽던 자리에서 고치는 것이 ①②의 본질이라 인라인이 주. 분할 뷰는 [[DESIGN#4. 금지 목록\|금지 3]](본문 영역 UI 침범)·읽기 폭 개념과 충돌해 기각. |
| D22 | 저장 시점 | **하이브리드** — 인라인 블록은 자동 저장, 소스 모드는 `⌘S` 명시 저장 | 자동 저장은 "미저장 상태"라는 개념 자체를 없애 탭 닫기 경고·세션 복원·크래시 손실 로직을 **불필요하게 만든다**. 위험이 큰 전체 편집만 명시 저장으로 통제권을 남긴다. |
| D22.1 | 더티 상태 봉인 | 소스 모드를 벗어나는 모든 경로(토글 해제·탭 전환·탭 닫기)에서 저장/취소를 확정 | 하이브리드가 되살린 더티 상태가 세션 복원·종료 경로까지 새어나가지 않게 한 화면 안에 가둔다. |
| D23 | 안전망 | **메모리 되돌리기만** — `⌘Z` + "문서를 연 시점으로 되돌리기". 디스크 스냅샷·`.bak` 없음 | 백업의 진짜 방어선은 앱이 아니라 볼트 바깥(git·Time Machine)에 있어야 한다. 앱의 책임은 "방금 한 실수의 즉시 취소"까지. |
| D24 | 편집 진입 | **전역 편집 모드 토글**(`⌘⌥E`, 기본 OFF, 상태 유지). 모드가 켜져 있을 때만 더블클릭이 편집을 연다 | **더블클릭은 이미 "단어 선택"**이다. 모드 없이 편집을 얹으면 읽기 중 우발적 편집이 상시로 일어나고 자동 저장이라 즉시 디스크에 박힌다. 호버 연필 버튼은 [[DESIGN#4. 금지 목록\|금지 3]]에 걸려 기각. |
| D25 | 메모 형식 | 블록 아래에 **콜아웃(`> [!note]`) 틀 삽입** | 메모가 콜아웃이면 화면에서 **"AI가 쓴 것"과 "내가 덧붙인 것"이 색으로 구분된다** — ②의 실질적 요구가 이것이다. 범용 "빈 블록 추가"와 구현 난도가 같다. |
| D26 | 편집 대상 블록 | **전부 균일하게** (문단·제목·리스트·인용·표·코드·수식·Mermaid) | 예외를 두면 경계를 문서로 설명해야 한다. 편집창을 나가면 즉시 재렌더되므로 표·다이어그램도 시행착오 루프가 짧다. |
| D27 | 릴리스 단계 | **2단계** — v0.11.0(모드+인라인+되돌리기) → 실사용 확인 → v0.12.0(소스 모드+메모 삽입) | 소스 모드는 **문서 전체를 한 번에 덮어쓰는 경로**라 실패 파장이 인라인과 질적으로 다르다. v0.1.0~v0.3.2에서 실사용 확인 없이 6개 릴리스가 연달아 깨졌던 교훈. |
| D28 | 대외 정체성 | **"읽기 중심 뷰어 — 필요할 때만 고친다"** | 편집 모드는 기본 OFF이고 켜지 않으면 동작이 종전과 완전히 같다. "뷰어 겸 편집기"는 Obsidian·VS Code와 같은 선반에 놓인 것처럼 읽혀 기대치를 잘못 세운다. |
| D29 | 저장 계산의 검증 | `edit-core.mjs`(순수 함수) + `test/edit-core.test.mjs` **Node 단독 테스트** | 실패의 최악이 "안 보임"에서 **"문서 손실"**로 격상되는 유일한 지점. GUI 검증(CDP)으로는 줄 계산의 엣지 케이스를 덮을 수 없다. |

## 2026-08-01 — 사이드바 맥락 분리 (G3 목업 5안)

폴더를 연 상태에서 트리 밖 파일을 열면 사이드바가 여전히 예전 폴더를 보여줘 혼동스럽다는
사용자 신고에서 출발. 목업은 [[g3-sidebar-context.html|design/g3-sidebar-context.html]] (현재 동작 + 4방향).

| # | 항목 | 결정 | 근거 / 비고 |
|---|------|------|-------------|
| D30 | 사이드바 구조 | **"따로 연 문서" 섹션을 트리 위에 둔다** (E안 — B의 축소판) | 혼동의 정체는 "읽는 문서가 화면 어디에도 없다"였다. 그 문서에 **자리를 만들어** 하이라이트가 의미를 되찾게 한다. 폴더 트리는 한 번도 흔들리지 않고, 섹션이 비면 숨어 **볼트만 쓸 때는 종전과 완전히 동일한 화면**이다. 기각: A(트리가 탭 추종 — 트리가 요동친다), B(루트 다중 — 파일 하나에 `~/Downloads` 수백 개가 딸려온다), C(단일 문서 모드 — 볼트 트리가 사라진다), D(배지만 — 원인이 남고 설명만 붙는다). |
| D31 | 상위 폴더 자동 열기 폐지 | 폴더가 열려 있지 않아도 **파일의 상위 폴더를 자동으로 열지 않는다** ([[DECISIONS\|D12·D18]] 개정) | 받은 파일 하나 때문에 `~/Downloads` 전체가 트리에 들어오던 동작이 같은 혼동의 다른 얼굴이었다. 이제 그 파일은 "따로 연 문서"에만 서고, 사이드바가 비지 않으므로 자동 열기의 원래 목적도 충족된다. |
| D32 | 세션 복원 | 세션에 `loose` 목록을 저장하고 **트리 밖 문서도 복원**한다 | 기존에는 복원 시 "트리 안에 있는 파일"만 통과시켜 **트리 밖 탭이 조용히 사라졌다**(재현 확인). D30과 뿌리가 같은 증상이라 함께 해소. |

## 2026-08-01 — 사용자 의견 수집

| # | 항목 | 결정 | 근거 / 비고 |
|---|------|------|-------------|
| D33 | 의견 수집 경로 | **도움말 메뉴 → 채워진 GitHub 이슈 작성 페이지를 브라우저로 연다** (`shell.openExternal`) | 앱이 이슈를 직접 등록하려면 토큰이 필요한데 **배포되는 앱에는 토큰을 넣을 수 없다**(누구나 꺼낼 수 있다). 서버를 두는 방식은 이 프로젝트 성격(단독 실행)과 맞지 않아 기각. 사용자가 자기 계정으로 올리므로 출처도 남는다. |
| D34 | 앱 내 입력 폼 | **두지 않는다** — 메뉴 항목 두 개가 전부 | "간단한 수정사항"을 받는 것이 목적이라 새 패널·상태 관리는 과하다. 작성은 GitHub 이슈 폼이 대신한다. |
| D35 | 자동 첨부 범위 | **앱 버전 · 플랫폼 · 아키텍처 · Electron 버전만.** 문서 경로·내용은 절대 첨부하지 않는다 | 공개 저장소로 올라간다. 진단에 필요한 최소치만 넣는다. |
| D36 | 계정 없는 경우 | **환경 정보 복사** 메뉴로 우회 | 이슈 제출에는 GitHub 계정이 필요하다. 서버 없이 이 제약을 없앨 방법은 없으므로, 다른 경로로 전달할 수 있게 최소한의 보조 수단만 둔다. |

## 2026-08-01 — 편집 2단계 구현 (D22·D25 이행)

| # | 항목 | 결정 | 근거 / 비고 |
|---|------|------|-------------|
| D37 | 소스 모드 진입점 | 메뉴(`⌘⌥S`) + **상태 바 버튼** 병행. `저장` 버튼은 미저장일 때만 나타난다 | [[DECISIONS\|D15]]와 같은 이유 — 메뉴 전용은 발견성이 낮고 자동 검증도 불가하다. 미저장 표시와 저장 수단을 한 자리에 두어 "저장 안 된 상태"가 눈에 보이게 한다. |
| D38 | 메모 단축키의 자리 | 메뉴 항목에는 **가속기를 두지 않고**, `⌘⇧M`을 **블록 편집창 안에서** 직접 처리한다 | 메뉴 가속기가 키를 가로채면 편집창까지 전달되지 않아 페이지 쪽 처리가 영원히 죽은 코드가 된다. 한 기능에 한 처리 지점만 둔다. |
| D39 | 소스 모드 중 외부 변경 | 화면을 갈아엎지 않고 **상태 바 알림만** 한다 | 재렌더하면 사용자가 쓰던 내용이 사라진다. 덮어쓸지 말지는 사용자가 저장 시점에 판단한다. |
| D40 | 소스 모드 이탈 확인 | main의 `dialog.showMessageBox` 2버튼(**저장** / 변경 버리기), 기본값 = 저장 | 데이터 손실 경로다. `window.confirm`은 버튼 라벨을 정할 수 없어 "취소 = 버리기"라는 위험한 매핑이 된다. |

## 2026-08-01 — 메모 우클릭 메뉴 (사용자 요청)

| # | 항목 | 결정 | 근거 / 비고 |
|---|------|------|-------------|
| D41 | 메모 진입점 추가 | 편집 모드에서 **블록 우클릭 → OS 네이티브 팝업 메뉴**("메모 달기") | 사용자 요청. 상시 표시되는 UI가 아니라 부를 때만 나타나므로 [[DESIGN#4. 금지 목록\|금지 3]](본문에 UI 크롬 침범 금지)에 걸리지 않는다. 자체 HTML 메뉴 대신 네이티브를 쓴 이유는 OS 관례와 일치시키기 위해서다. |
| D42 | 삽입 경로 분기 | 편집창에서 부르면 **고치던 내용까지 저장**하고, 우클릭으로 부르면 **그 블록은 손대지 않는다** | 우클릭은 편집창을 열지 않은 블록에서 누르므로 "편집 중 텍스트"가 없다. 두 경로가 `insertMemoAfter()` 하나로 합류한다. |
| D43 | 팝업 메뉴 참조 유지 | `Menu` 객체를 모듈 수준 변수에 붙잡아 둔다 | 지역 변수로 두면 팝업이 떠 있는 동안 GC 대상이 되는 Electron의 알려진 함정이다. |

## 미결

- (없음 — 구현 중 발견되는 엣지 케이스는 G5 규칙에 따라 보수적 선택 + [[implementation-notes]] 기록)
