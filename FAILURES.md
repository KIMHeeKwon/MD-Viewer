# FAILURES — MD-Viewer-ALL

## Active Rules

1. **릴리스는 매트릭스 시작 전 선행 잡(create-release)에서 단독 생성한다.**
   macOS·Windows 러너가 동시에 `electron-builder --publish always`를 실행하면 각자
   릴리스를 만들어 같은 태그에 릴리스 2개가 생긴다. `releaseType: "release"` 설정만으로는
   불충분함이 v0.2.0에서 실증됨 — 두 러너가 "없음 확인 → 생성"을 동시에 수행하는 TOCTOU 경합.
   `.github/workflows/release.yml`의 create-release 잡이 먼저 릴리스를 만들고 package 잡은 `needs`로 대기한다.
   `build.publish.releaseType: "release"`는 기존 릴리스를 태그로 찾게 하기 위해 유지.

2. **릴리스 워크플로는 electron-builder 전에 반드시 `npm run bundle`을 실행한다.**
   `renderer-dist/`는 gitignore 대상이라 CI 체크아웃에 존재하지 않는다. 번들 단계 없이
   패키징하면 `files` 글롭의 `renderer-dist/**/*`가 빈 것을 잡아, 설치본이 스타일·스크립트
   없이 뼈대 HTML만 표시된다. dev 실행(`npm start`)은 bundle을 포함하므로 이 결함이
   가려진다 — **패키징 산출물은 반드시 설치·실행까지 검증**할 것(asar에 `renderer-dist` 포함 확인).

3. **정리(cleanup) 코드가 사용자 조작 경로(닫기·삭제·취소) 안에 있으면 반드시 try/catch로 감싼다.**
   자원 해제 실패는 로그 대상이지 조작 차단 사유가 아니다.

4. **외부 라이브러리 메이저 버전을 올리거나 새로 도입할 때, 사용하는 API가 해당 버전에 실제로
   존재하는지 확인한다**(타입 정의·소스 grep). 특히 `destroy`·`cleanup` 같은 생명주기 메서드는
   버전 간 변경이 잦다.

5. **`grep`은 NUL 바이트가 있는 파일을 이진으로 보고 매치를 조용히 숨긴다.** 소스에 원시 제어문자가
   섞이면 검증이 **거짓 음성**을 낸다(있는 코드를 "없다"고 판정). 검증이 예상과 다르게 "없음"으로
   나오면 grep 결과를 곧이 믿지 말고 `python`/`node`로 파일을 읽어 재확인한다. 소스에 제어문자를
   넣지 말고 `\u0000` 같은 escape로 표기한다.

6. **시각화·목록 기능은 규모가 다른 실제 데이터로 검증한다.** 작은 표본(문서 10개)에서
   잘 보이던 그래프가 실제 볼트(문서 39개·연결 383개)에서는 중앙에 뭉쳐 라벨이 겹쳐
   읽을 수 없었다. 힘·투명도·라벨 같은 파라미터는 **개수에 따라 스케일**해야 하며,
   고정값은 표본 크기에서만 옳다.

7. **입자 사이의 힘은 작용·반작용이 같아야 한다.** 한쪽에만 가중치를 곱하면(예: 허브 고정을
   막으려 간선 힘을 차수로 나누기) 전체에 **순수 운동량**이 남아 화면이 한 방향으로 계속 흐른다.
   비대칭 가중이 꼭 필요하면 매 틱 **평균 속도를 빼서** 표류를 제거한다.

8. **물리 기반 애니메이션은 반드시 냉각(수렴)시킨다.** 끝없이 도는 시뮬레이션은 "화면이 가만히
   있지 않는" 사용성 문제이자 CPU 낭비다. 안정 후에는 계산을 멈추고, 조작이 있을 때만 되살린다.

## 사례 기록

### 2026-07-24 — v0.1.0 릴리스 드래프트 분열
- 증상: Release 워크플로 성공했으나 자산이 드래프트 2개(맥 4개 / 윈 3개+깨진 이름 1개)로 분산.
- 원인: 위 Active Rule 1의 경합. electron-builder 기본값이 draft 생성이기 때문.
- 조치: 자산 수동 병합 후 공개, `releaseType: "release"` 설정 추가. 다음 태그에서 검증 필요.

### 2026-07-24 — v0.2.0에서 경합 재발 (`releaseType: release`로 불충분 판명)
- 증상: 공개 릴리스 2개가 같은 태그에 생성. 자산 분산 + 깨진 이름 blockmap 재현.
- 원인: 두 러너가 "릴리스 없음 확인 → 생성"을 동시 수행 (TOCTOU). releaseType은 조회 방식만
  바꿀 뿐 생성 원자성을 보장하지 않음.
- 조치: 수동 병합 2회차 수행 후, `release.yml`에 create-release 선행 잡 추가 (Active Rule 1 개정).
- **검증 완료 (v0.2.1)**: 선행 잡 구조로 단일 릴리스에 자산 8개가 정상 집결. 경합 종결.
  잔여 특이점: zip 블록맵 자산명이 `MD.Viewer-...`로 보이는 것은 productName의 공백을
  GitHub가 점으로 치환한 것 — 무해 (자동 업데이트 델타용 파일).

### 2026-07-25 — 설치본에서 GUI·기능 전무 (`renderer-dist` 누락)
- 증상: v0.3.2 설치본 실행 시 스타일·스크립트 없이 뼈대 HTML만 표시. 그동안의 기능이 하나도 안 보임.
- 진단: 설치 앱의 app.asar(199KB, 정상은 ~8MB)에 `renderer-dist/`가 없음. [[src/renderer/index.html]]이
  참조하는 `../../renderer-dist/renderer.{js,css}`가 404 → 무스타일·무기능.
- 원인: `release.yml`이 `npm ci` 후 곧바로 electron-builder 실행, `npm run bundle` 누락.
  `renderer-dist`는 gitignore라 CI에 미존재 → 패키지에서 통째 누락. v0.1.0~v0.3.2 전부 동일.
  dev 실행(`npm start`)에만 의존해 검증한 탓에 6개 릴리스 동안 미발견.
- 조치: `release.yml`에 "Build renderer" 단계 추가. 로컬 `npm run dist`로 asar에 renderer-dist
  포함(8.3MB) 검증 후 v0.3.3 재릴리스. Active Rule 2 승격.

### 2026-07-29 — PDF 탭이 닫히지 않음 (제거된 라이브러리 API 호출)
- 증상: PDF 탭의 × 버튼이 아무 반응 없음. 마크다운 탭은 정상.
- 원인: `closeTab`이 `pdfDoc.destroy()`를 호출했으나 **pdfjs-dist 6에는 `PDFDocumentProxy.destroy()`가 없다**
  (`loadingTask.destroy()`로 대체됨). `TypeError`가 발생하며 그 아래의 탭·패널 제거 코드에 도달하지 못했다.
  v0.2.0(PDF 열람 도입) 이후 계속 존재했으나, 탭을 닫는 조작을 테스트하지 않아 미발견.
- 조치: `pdfDoc.loadingTask?.destroy()`로 교체하고 **try/catch로 감쌌다** — 자원 해제 실패가
  사용자의 닫기 조작을 막아서는 안 되기 때문. CDP로 실제 앱을 자동 조작해 3개 시나리오
  (PDF 단독 / 마크다운 단독 / PDF+마크다운 혼합) 재현·검증.

- **승격**: 위 [[FAILURES#Active Rules|Active Rules]] 3·4로 반영.

### 2026-07-29 — 검증의 거짓 음성: NUL 바이트가 grep을 침묵시켰다
- 증상: 설치본과 소스 모두에서 방금 추가한 업데이트 코드(`update:check` 등)가 **"없다"고 검증됨.**
  코드가 유실된 것으로 오판해 원인을 추적했다.
- 실제: 코드는 정상적으로 있었다. `src/main.js`의 그래프 간선 중복 키에 **원시 NUL 문자**가
  구분자로 들어가 있어(`` `${f}\x00${target}` ``) grep이 파일을 이진으로 판정하고 매치를 숨긴 것이다.
  앱 동작에는 영향이 없었다 — 패키징 앱에서 업데이트 확인이 실제로 성공한 것이 반증이었는데도
  grep 결과를 더 신뢰해 헛수고를 했다.
- 조치: NUL을 `\u0000` escape로 바꿔 평문 파일로 만들고, Active Rule 5로 승격.
- 교훈: **검증 도구의 침묵과 "대상 없음"을 구분해야 한다.** 이미 성공적으로 동작한 증거(패키징 앱의
  실제 조회 성공)가 있는데도 정적 검색 결과를 우선한 것이 판단 오류였다.

### 2026-07-30 — 실제 볼트 규모에서 그래프가 읽을 수 없었다
- 증상: 사용자 폴더(문서 39·연결 383)에서 노드가 중앙에 뭉치고 라벨이 서로 겹쳐 판독 불가.
- 원인: ① 반발력·링크거리가 노드 수와 무관한 고정값 → 간선 수가 늘면 스프링이 반발을 압도.
  ② 허브가 모든 간선 힘을 그대로 받아 중앙에 박힘. ③ 라벨을 전부 그려 충돌.
  ④ 간선 투명도 고정(0.55)으로 383개가 배경을 뒤덮음.
- 조치: 힘 파라미터를 노드 수에 스케일, 간선 힘을 차수로 정규화, 라벨 충돌 회피(연결 많은 순,
  노드 원도 점유 영역으로 취급), 간선 투명도를 간선 수에 반비례, 노드 최소 간격 분리력 추가.
  1차 수정에서 투명도를 과하게 낮춰(0.08) 구조가 안 보였고 이를 재조정(0.14~0.5).
- 부수 발견: 하위 폴더에 **같은 파일명이 중복**되면 위키링크가 하나로만 해석되어 나머지가
  고립됐다. Obsidian 규칙(같은 폴더 우선 → 후보 1개 → 루트에 가까운 것)으로 해석하고,
  중복 이름은 그래프에서 `이름 (폴더)`로 구분 표시하도록 수정.
- 규칙 승격: Active Rule 6.

### 2026-07-30 — 그래프가 한 방향으로 계속 흘렀다
- 증상: 그래프가 고정되지 않고 계속 한쪽으로 이동. 사용자 신고.
- 원인: 직전 수정에서 허브가 중앙에 박히는 것을 막으려 간선 힘을 노드 차수로 나눴는데
  (`fx * wa` vs `fx * wb`, wa≠wb) **작용·반작용이 깨져 시스템 전체에 운동량이 생겼다.**
  중력 목표도 클러스터 무게중심이라 절대 기준점이 없어 표류를 되돌릴 힘이 없었다.
  즉 앞선 두 수정(차수 정규화·무게중심 중력)이 결합해 만든 부작용이다.
- 조치: ① 매 틱 **평균 속도를 빼서** 순수 표류만 제거(내부 상대 운동은 보존).
  ② 냉각 계수(alpha)를 도입해 약 300틱에 걸쳐 0으로 수렴시키고, 수렴 후에는 물리 계산을 멈춤.
  드래그·재배치·범위 전환 시에만 되살린다.
- 검증: 캔버스 밝은 픽셀의 무게중심을 3초 간격으로 5회 측정 — 후반 9초간 Δx=Δy=**0.0px**.
- 규칙 승격: Active Rule 7·8.

## 프로토콜 우회 로그

(없음)
