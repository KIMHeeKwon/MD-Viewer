# DESIGN.md — MD Viewer 디자인 토큰 (G3 수렴 확정)

> 방향: **C. IDE 다크** (G3 목업 4안 중 선택, 2026-07-24).
> 레퍼런스: `design/g3-directions.html`의 `.dC` 프레임. Tokyo Night 팔레트 계열.
> 이 파일이 색·타이포·간격의 단일 원천이다. 구현은 여기 정의된 토큰만 사용한다.

## 1. 색 토큰

| 토큰 | 다크(기본) | 라이트 | 용도 |
|------|-----------|--------|------|
| `--bg` | `#1a1b26` | `#f7f8fb` | 본문 배경 |
| `--fg` | `#c0caf5` | `#2a2e3f` | 본문 텍스트 |
| `--dim` | `#565f89` | `#8a91a3` | 보조 텍스트, 비활성 |
| `--side` | `#16161e` | `#eef0f5` | 사이드바·탭바·타이틀 영역 |
| `--line` | `#101014` | `#d5d9e4` | 경계선 |
| `--accent` | `#7aa2f7` | `#2e5cc5` | 링크·활성 탭 표시·선택 강조 |
| `--sel` | `#292e42` | `#dfe4f0` | 트리 선택 배경 |
| `--soft` | `#24283b` | `#e4e7ee` | 인라인 코드·표 헤더 배경 |
| `--code-bg` | `#16161e` | `#16161e` | 코드 블록 배경 (**라이트에서도 다크 유지** — C 방향의 정체성) |
| `--callout-bg` | `#24283b` | `#e9edf5` | 콜아웃 배경 |

- 코드 하이라이트: highlight.js `tokyo-night-dark` 테마 고정 (양 테마 공통).
- Mermaid: 다크 테마 시 `dark`, 라이트 시 `default` 프리셋.

## 2. 타이포그래피

| 역할 | 스택 | 크기/행간 |
|------|------|-----------|
| 문서 본문 | 시스템 산세리프 (`-apple-system`, `Segoe UI`, `Apple SD Gothic Neo`, `Malgun Gothic`) | 15px / 1.75 |
| 문서 H1 | 본문과 동일 계열, 700 | 28px |
| 문서 H2 | 동일, 650 | 21px, 하단 1px 경계선 |
| 코드·상태 바 | `JetBrains Mono`, `SF Mono`, `Cascadia Code`, `Consolas`, monospace | 13px / 1.6 |
| UI (트리·탭) | 시스템 산세리프 | 13px |

## 3. 레이아웃

- 사이드바(트리): 폭 240px, 접기 가능. 트리 항목 높이 26px.
- 탭바: 높이 36px, VS Code식 사각 탭 + 활성 탭 상단 2px 액센트 바.
- 상태 바: 하단 24px, 액센트 배경, 모노스페이스. 내용: 파일 경로 · 인코딩 · 문법 모드 · 단어 수 · 감시 상태 · 테마 토글.
- 본문 컬럼: 최대 폭 860px, 패딩 40px, 중앙 정렬.
- 표·코드·수식: 자체 `overflow-x: auto` (본문 가로 스크롤 금지).

## 4. 금지 목록

1. **네트워크 요청 금지** — 폰트·CSS·JS 전부 번들. CDN 참조 금지 (완전 오프라인 동작).
2. 토큰 외 임의 색 추가 금지.
3. 본문 영역에 UI 크롬 침범 금지 (버튼·배지 등은 사이드바/상태 바에만).
4. 애니메이션은 테마 전환 fade 1건 외 금지.
