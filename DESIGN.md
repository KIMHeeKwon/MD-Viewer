# DESIGN — MD Viewer 디자인 토큰

> **원천: KKOBAK (HKDS) 디자인 시스템** — `hk-design-system@0.1.0` (2026-08-17 적용).
> 이전 방향(C. IDE 다크 / Tokyo Night 자체 팔레트, 2026-07-24~2026-08-15)을 대체한다.
> 이관 패키지: `design_handoff_kkobak_ds/`. 구현은 `src/renderer/kkobak/`에 복사해 둔 두 파일.
>
> **이 파일은 "우리 앱이 DS를 어떻게 쓰는가"의 단일 원천이다.** 토큰 *값*의 원천은 DS이며,
> 값을 이 문서에 옮겨 적지 않는다 — 값은 DS가 바뀌면 따라 바뀐다.

---

## 1. 적용 범위 — 무엇을 가져왔고 무엇을 안 가져왔나

KKOBAK은 **React 컴포넌트 라이브러리**(`window.HKDS.*`, 9종)와 **토큰 체계**로 이뤄져 있다.
이 앱은 React를 쓰지 않는 바닐라 렌더러이므로 **토큰 체계만 채택**했다.

| 가져온 것 | 안 가져온 것 |
|---|---|
| 색·타이포·간격·라운드·그림자 토큰 전체 | React 컴포넌트 9종 (Button·Card·Dialog…) |
| Space Grotesk 폰트 (woff2 내장) | `_ds_bundle.js` (컴포넌트 번들) |
| `data-theme="light"` 오버라이드 체계 | `ThemeProvider` 래핑 |

컴포넌트를 쓰려면 React를 도입하고 렌더러 UI를 전면 재작성해야 한다. 그것은 디자인 적용이
아니라 재작성이므로 범위 밖으로 두었다. **DS가 제시하는 시각 언어는 토큰만으로 충분히 구현된다.**

---

## 2. 로드 구조

```
styles.css
  ├── @import kkobak/_ds_bundle.css    ← 해시 변수(--_1ukhvxl*) 정의 + Space Grotesk @font-face 3종
  ├── @import kkobak/theme-tokens.css  ← --k-* 별칭 + [data-theme="light"] 오버라이드
  └── :root { ... }                    ← 앱 의미 토큰을 --k-* 에 매핑
```

esbuild가 세 파일을 `renderer-dist/renderer.css` 하나로 번들한다. 폰트는 base64로 CSS 안에
들어 있어 네트워크 로드가 없다 ([[DESIGN#5. 금지 목록|금지 1]] 충족).

> **CSP 주의**: 폰트가 `data:` URI이므로 `index.html`의 `font-src`에 `data:`가 있어야 한다.
> 빠지면 Space Grotesk가 조용히 폴백된다 (2026-08-17 실제로 걸렸던 지점).

### 해시 변수를 직접 쓰지 않는다

DS는 vanilla-extract로 빌드되어 변수명이 `--_1ukhvxl7` 같은 해시다. **DS 버전이 오르면 이 이름이
바뀐다.** 앱 코드는 반드시 `--k-*` 별칭만 참조한다.

---

## 3. 앱 의미 토큰 ↔ DS 토큰 매핑

앱 CSS는 기존의 의미 이름(`--bg`, `--fg` …)을 그대로 쓴다. 그 이름들이 DS 토큰을 가리키도록
한 겹 매핑했다. **덕분에 900줄이 넘는 기존 스타일을 건드리지 않고 시각 언어만 교체됐다.**

| 앱 토큰 | DS 토큰 | 용도 |
|---|---|---|
| `--bg` | `--k-bg` | 본문 배경 |
| `--fg` | `--k-text` | 본문 텍스트 |
| `--dim` | `--k-muted` | 보조 텍스트·비활성 |
| `--side` | `--k-surface` | 사이드바·탭바 |
| `--line` | `--k-border` | 경계선 |
| `--accent` | `--k-accent` | 링크·활성 표시 (**teal**) |
| `--accent2` | `--k-accent2` | **위키링크** (violet) — 외부 링크와 구분 |
| `--sel` | `--k-surface-hover` | 선택 배경 |
| `--soft` | `--k-surface` | 인라인 코드·표 헤더 |
| `--callout-bg` | `--k-surface` | 콜아웃 배경 |
| `--status-fg` | `--k-accent-text` | 상태 바 글자 (배경이 accent이므로 DS의 대비색) |
| `--danger` / `--success` / `--warning` | 동명 DS 토큰 | 콜아웃 톤·오류 표시·검색 하이라이트 |
| `--radius-sm` / `--radius-md` | `--k-radius-sm` / `--k-radius-md` | 코드 블록·콜아웃·편집창 |

**테마별 블록이 하나뿐이다.** `--k-*`가 `[data-theme]`에 따라 스스로 바뀌므로, 예전처럼
다크·라이트 두 벌을 유지할 필요가 없어졌다.

### 3.1 사이드바가 본문보다 밝아졌다

이전 팔레트는 사이드바(`--side`)가 본문보다 **어두웠다**. DS의 `surface`는 배경 위에 얹힌
**표면**이라 본문보다 밝다. 의도된 변화이며, 레이어 관계가 반대로 뒤집힌 것이다.

### 3.2 유일한 예외 — 코드 블록

```css
--code-bg: #141A2E;   /* KKOBAK 다크 surface 고정 */
--code-fg: #E6ECFF;   /* KKOBAK 다크 text 고정 */
```

두 테마 모두 **어둡게 고정**한다. highlight.js 테마를 `tokyo-night-dark`로 고정해 쓰기 때문에,
밝은 배경에 올리면 신택스 색이 보이지 않는다. `--k-*`는 라이트에서 밝은 값으로 바뀌므로
여기서는 참조할 수 없어 값을 직접 적었다 — **DS를 업그레이드하면 `theme-tokens.css`의 다크
값과 대조해 갱신할 것.**

---

## 4. 타이포그래피

| 역할 | 토큰 | 비고 |
|---|---|---|
| 본문·UI | `--k-font-body` | Space Grotesk → system-ui → … |
| 코드·상태 바 | `--k-font-mono` | JetBrains Mono → SF Mono → Menlo |

> **JetBrains Mono는 번들에 내장돼 있지 않다.** 핸드오프 README는 내장이라고 적었지만
> `@font-face`는 Space Grotesk 400/500/700 세 개뿐이다(2026-08-17 실측). 코드 폰트는
> OS의 `SF Mono`/`Menlo`로 폴백된다 — 이전과 동일한 결과라 실질 영향은 없다.

**한글은 Space Grotesk에 없어 스택 다음 항목(`system-ui`)으로 폴백된다.** 라틴 문자만
Space Grotesk로 렌더링되는 혼합 조판이며, 이는 DS 스택의 의도된 동작이다.

문서 본문 크기(15px 기본)와 읽기 폭은 앱이 `--doc-font-size`·`--read-width`로 별도 관리한다.
사용자가 조절하는 값이라 DS 토큰으로 고정하지 않는다.

---

## 5. 금지 목록

1. **렌더링 자산은 전부 번들** — 폰트·CSS·JS를 앱에 포함하고 CDN을 참조하지 않는다.
   근거는 오프라인 교리가 아니라 **렌더링 충실도와 견고성**이다 ([[DECISIONS|D16]]).
2. **토큰 외 임의 색 추가 금지.** hex 하드코딩 금지, **해시 변수(`--_1ukhvxl*`) 직접 참조 금지.**
   예외는 §3.2(코드 블록)와 인쇄 CSS뿐이며, 둘 다 이유가 주석에 적혀 있다.
3. 본문 영역에 UI 크롬 침범 금지 (버튼·배지 등은 사이드바/상태 바에만).
   - **대상은 "상시 표시되는" 것이다.** 부를 때만 나타났다 사라지는 것 — 블록 우클릭 메뉴
     ([[DECISIONS|D41]]), 편집 모드의 옅은 호버 강조, 편집창 자체 — 은 대상이 아니다.
   - 이 규칙 때문에 기각된 것: 블록 호버 시 나타나는 연필 버튼. 대신 **편집 모드 분리**로 풀었다.
4. 애니메이션은 테마 전환 fade 1건 외 금지.
5. 접기·펼침 등의 상태 표시는 **텍스트 글리프**(`▾`/`▸`)로 한다. 아이콘 폰트·SVG 아이콘 금지.
6. **DS 컴포넌트를 div로 재현하지 않는다.** 필요해지면 React 도입 여부를 먼저 결정한다
   (지금은 채택하지 않았다 — §1).

---

## 6. 레이아웃

- 사이드바(트리): 폭 240px, 드래그 조절. 트리 항목 높이 26px.
  - 트리 **위**에 "따로 연 문서" 섹션 — 해당 문서가 없으면 통째로 숨는다 ([[DECISIONS|D30]]).
- 탭바: 높이 36px, 사각 탭 + 활성 탭 상단 2px 액센트 바.
- 상태 바: 하단 24px, `--accent` 배경.
  내용: 파일 경로 · 인코딩 · 문법 모드 · 단어 수 · 감시 상태 · 읽기/편집 · 소스 · 저장 ·
  되돌리기 · 글자 크기 · 테마 토글.
  - 배경이 `--accent`이므로 **강조를 액센트 색으로 표현할 수 없다.** 켜진 상태는
    `--bg` 배경 + `--fg` 글자의 알약으로 반전시킨다.
- 본문 컬럼: 최대 폭 `--read-width`(기본 860px), 중앙 정렬.
- 표·코드·수식: 자체 `overflow-x: auto` (본문 가로 스크롤 금지).

---

## 7. 인쇄 (PDF 내보내기)

`@media print`는 화면 테마와 무관하게 **항상 라이트**로 강제한다. 이때는 `--k-*`를 쓸 수 없다
(현재 테마를 따라가 버린다). 그래서 KKOBAK 라이트 값을 **직접 적어 둔 고정 팔레트**를 쓴다.
DS 업그레이드 시 §3.2와 함께 갱신 대상이다.

---

## 8. DS 업그레이드 절차

1. 새 `_ds_bundle.css` · `theme-tokens.css`를 `src/renderer/kkobak/`에 덮어쓴다.
2. `_ds_bundle.css`의 `:root` 블록을 열어 **해시 변수의 순서·개수**가 같은지 확인한다
   (`theme-tokens.css` 상단 주석에 0..1d 대응이 적혀 있다).
3. **§3.2의 코드 블록 고정값**과 **§7 인쇄 팔레트**를 새 다크/라이트 값과 대조해 갱신한다.
4. 두 테마에서 본문·사이드바·상태 바·콜아웃 3종·코드 블록 색을 실측한다
   (CDP로 `getComputedStyle` 확인 — 2026-08-17에 쓴 방법).

---

## 9. 관련 문서

- [[ARCHITECTURE]] — 시스템 구조 (이 문서는 그중 시각 층만 다룬다)
- [[DECISIONS]] — 결정 이력
- `design_handoff_kkobak_ds/README.md` — DS 원본 핸드오프 문서 (토큰 값 표 포함)
